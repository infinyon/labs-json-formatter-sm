transforms:
  - uses: infinyon-labs/json-formatter@0.1.0
    with:
      spec:
        default:
          format: 
            with: "{} ({}) submitted a request"
            using: 
              - "/name"
              - "/email"
            output: "/formatted"